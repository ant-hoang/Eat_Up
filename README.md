# Eat Up

## Database Schema Design

![Eat_It Schema](https://github.com/ant-hoang/Eat_Up/assets/105393737/55d029ba-3670-419b-85d2-d3c66ef1c3c0)

## API Documentation

## USER AUTHENTICATION/AUTHORIZATION

### All endpoints that require authentication

All endpoints that require a current user to be logged in.

* Request: endpoints that require authentication
* Error Response: Require authentication
  * Status Code: 401
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Authentication required"
    }
    ```

### All endpoints that require proper authorization

All endpoints that require authentication and the current user does not have the
correct role(s) or permission(s).

* Request: endpoints that require proper authorization
* Error Response: Require proper authorization
  * Status Code: 403
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Forbidden"
    }
    ```

### Get the Current User

Returns the information about the current user that is logged in.

* Require Authentication: true
* Request
  * Method: GET
  * URL: /users/current
  * Body: none

* Successful Response when there is a logged in user
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "user": {
        "id": 1,
        "firstName": "John",
        "lastName": "Smith",
        "email": "john.smith@gmail.com",
        "username": "JohnSmith"
      } 
    }
    ```

* Successful Response when there is no logged in user
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "user": null
    }
    ```

### Log In a User

Logs in a current user with valid credentials and returns the current user's
information.

* Require Authentication: false
* Request
  * Method: POST
  * URL: /auth/login
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "credential": "john.smith@gmail.com",
      "password": "secret password"
    }
    ```

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "user": {
        "id": 1,
        "firstName": "John",
        "lastName": "Smith",
        "email": "john.smith@gmail.com",
        "username": "JohnSmith"
      } 
    }
    ```

* Error Response: Invalid credentials
  * Status Code: 401
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Invalid credentials"
    }
    ```

* Error response: Body validation errors
  * Status Code: 400
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bad Request", // (or "Validation error" if generated by Sequelize),
      "errors": {
        "credential": "Email or username is required",
        "password": "Password is required"
      }
    }
    ```

### Sign Up a User

Creates a new user, logs them in as the current user, and returns the current
user's information.

* Require Authentication: false
* Request
  * Method: POST
  * URL: /auth/signup
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "firstName": "John",
      "lastName": "Smith",
      "email": "john.smith@gmail.com",
      "username": "JohnSmith",
      "password": "secret password"
    }
    ```

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "user": {
        "id": 1,
        "firstName": "John",
        "lastName": "Smith",
        "email": "john.smith@gmail.com",
        "username": "JohnSmith"
      }
    }
    ```

* Error response: User already exists with the specified email
  * Status Code: 500
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "User already exists",
      "errors": {
        "email": "User with that email already exists"
      }
    }
    ```

* Error response: User already exists with the specified username
  * Status Code: 500
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "User already exists",
      "errors": {
        "username": "User with that username already exists"
      }
    }
    ```

* Error response: Body validation errors
  * Status Code: 400
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bad Request", // (or "Validation error" if generated by Sequelize),
      "errors": {
        "email": "Invalid email",
        "username": "Username is required",
        "firstName": "First Name is required",
        "lastName": "Last Name is required"
      }
    }
    ```

## RECIPES

### Get all Recipes

Return all the recipes.

* Require Authentication: false
* Request
  * Method: GET
  * URL: /recipes
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "Recipes": [
        {
          "id": 1,
          "user_id": 1,
          "title": "Breakfast Special",
          "description": "Here are the steps of making this dish",
          "created_at": "2021-11-19 20:39:36",
          "updated_at": "2021-11-19 20:39:36",
          "num_likes": 3,
          "preview_image": "my_dish.png",
          "video_url": "my_dish.avi"
        },
        {
          "id": 2,
          "user_id": 2,
          "title": "Lunch Special",
          "description": "Hope you enjoy this recipe",
          "created_at": "2021-11-19 20:39:36",
          "updated_at": "2021-11-19 20:39:36",
          "num_likes": 4,
          "preview_image": "lunch_dish.png",
          "video_url": "lunch_dish.avi"
        }
      ]
    }
    ```

### Get all Recipes owned by the Current User

Returns all the recipes owned (created) by the current user.

* Require Authentication: true
* Request
  * Method: GET
  * URL: /recipes/current
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "Recipes": [
        {
          "id": 1,
          "user_id": 1,
          "title": "Breakfast Special",
          "description": "Here are the steps of making this dish",
          "created_at": "2021-11-19 20:39:36",
          "updated_at": "2021-11-19 20:39:36",
          "num_likes": 3,
          "preview_image": "my_dish.png",
          "video_url": "my_dish.avi"
        }
      ]
    }
    ```

### Get details of a Recipe from an id

Returns the details of a recipe specified by its id.

* Require Authentication: false
* Request
  * Method: GET
  * URL: /recipes/:recipeId
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "id": 1,
      "user_id": 1,
      "title": "Breakfast Special",
      "origin": "American",
      "description": "Here are the steps of making this dish",
      "directions": "1. Cook eggs, 2. Bake Bread, 3. Add Milk",
      "created_at": "2021-11-19 20:39:36",
      "updated_at": "2021-11-19 20:39:36",
      "num_likes": 3,
      "preview_image": "my_dish.png",
      "video_url": "my_dish.avi",
      "User": {
        "id": 1,
        "username": "JohnSmith"
      },
      "Ingredients": [
        {
          "id": 1,
          "name": "eggs",
          "quantity": 2,
          "metric": null
        },
        {
          "id": 2,
          "name": "milk",
          "quantity": 1.5,
          "metric": "cups"
        },
        {
          "id": 3,
          "name": "bread",
          "quantity": 5,
          "metric": "oz"
        }
      ]
    }
    ```
* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

### Create a Recipe

Creates and returns a new recipe.

* Require Authentication: true
* Request
  * Method: POST
  * URL: /recipes
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "title": "My New Recipe",
      "description": "It's a healthy one",
      "origin": "Asian",
      "directions": "1. Prepare broth, 2. Season chicken, 3. Add it together",
      "video": "new_dish.mpeg",
      "images": "new_dish.img"
    }
    ```

* Successful Response
  * Status Code: 201
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "id": 1,
      "user_id": 1,
      "title": "My New Recipe",
      "description": "It's a healthy one",
      "origin": "Asian",
      "directions": "1. Prepare broth, 2. Season chicken, 3. Add it together",
      "video": "new_dish.mpeg",
      "images": "new_dish.img",
      "created_at": "2022-12-25 20:39:36",
      "updated_at": "2022-12-25 20:39:36",
    }
    ```

* Error Response: Body validation error
  * Status Code: 400
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bad Request", // (or "Validation error" if generated by Sequelize),
      "errors": {
        "title": "Title is required",
        "description": "Description is required",
        "origin": "Origin is required",
        "directions": "Directions are required",
        "images": "One image is required"
      }
    }
    ```

### Add an Ingredient to a Recipe based on the Recipe's id

Create and return a new ingredient for a recipe specified by id.

* Require Authentication: true
* Require proper authorization: Spot must belong to the current user
* Request
  * Method: POST
  * URL: /recipes/:recipeId/ingredients/new
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "name": "jasmine rice",
      "quantity": 4,
      "metric": "cups"
    }
    ```

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "id": 1,
      "name": "jasmine rice",
      "quantity": 4,
      "metric": "cups"
    }
    ```

* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

### Edit a Recipe

Updates and returns an existing recipe.

* Require Authentication: true
* Require proper authorization: Recipe must belong to the current user
* Request
  * Method: PUT
  * URL: /recipes/:recipeId
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "title": "My Better Recipe",
      "description": "It's a less healthy one",
      "origin": "Asian",
      "directions": "1. Prepare broth, 2. Season pork, 3. Add it together",
      "video": "new_dish.mpeg",
      "images": "new_dish.img"
    }
    ```

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "id": 1,
      "user_id": 1,
      "title": "My Better Recipe",
      "description": "It's a less healthy one",
      "origin": "Asian",
      "directions": "1. Prepare broth, 2. Season pork, 3. Add it together",
      "video": "new_dish.mpeg",
      "images": "new_dish.img",
      "created_at": "2022-12-25 20:39:36",
      "updated_at": "2022-12-29 20:39:36",
    }
    ```

* Error Response: Body validation error
  * Status Code: 400
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bad Request", // (or "Validation error" if generated by Sequelize),
      "errors": {
        "title": "Title is required",
        "description": "Description is required",
        "origin": "Origin is required",
        "directions": "Directions are required",
        "images": "One image is required"
      }
    }
    ```

* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

### Delete a Recipe

Deletes an existing recipe.

* Require Authentication: true
* Require proper authorization: Recipe must belong to the current user
* Request
  * Method: DELETE
  * URL: /recipes/:recipeId
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Successfully deleted"
    }
    ```

* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

## COMMENTS

### Get all Comments of the Current User

Returns all the comments written by the current user.

* Require Authentication: true
* Request
  * Method: GET
  * URL: /comments/current
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "Comments": [
        {
          "id": 1,
          "userId": 1,
          "recipeId": 1,
          "comment": "This was an amazing dish!",
          "createdAt": "2021-11-19 20:39:36",
          "updatedAt": "2021-11-19 20:39:36" ,
          "User": {
            "id": 1,
            "firstName": "John",
            "lastName": "Smith"
          },
          "Recipe": {
            "id": 1,
            "title": "My Better Recipe",
            "description": "It's a less healthy one",
            "origin": "Asian",
            "directions": "1. Prepare broth, 2. Season pork, 3. Add it together",
            "video": "new_dish.mpeg",
            "images": "new_dish.img",
          },
        }
      ]
    }
    ```

### Get all Comments by a Recipe's id

Returns all the comments that belong to a recipe specified by id.

* Require Authentication: false
* Request
  * Method: GET
  * URL: /recipes/:recipeId/comments
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "Comments": [
        {
          "id": 1,
          "userId": 1,
          "recipeId": 1,
          "comment": "This was an awesome recipe!",
          "createdAt": "2021-11-19 20:39:36",
          "updatedAt": "2021-11-19 20:39:36" ,
          "User": {
            "id": 1,
            "firstName": "John",
            "lastName": "Smith"
          },
        }
      ]
    }
    ```

* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

### Create a Comment for a Recipe based on the Recipe's id

Create and return a new comment for a recipe specified by id.

* Require Authentication: true
* Request
  * Method: POST
  * URL: /recipes/:recipeId/comments
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "comment": "This is an amazing recipe!"
    }
    ```

* Successful Response
  * Status Code: 201
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "id": 1,
      "userId": 1,
      "recipeId": 1,
      "review": "This is an awesome recipe!",
      "createdAt": "2021-11-19 20:39:36",
      "updatedAt": "2021-11-19 20:39:36"
    }
    ```

* Error Response: Body validation errors
  * Status Code: 400
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bad Request", // (or "Validation error" if generated by Sequelize),
      "errors": {
        "comment": "Comment text is required",
      }
    }
    ```

* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

* Error response: Review from the current user already exists for the Recipe
  * Status Code: 500
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "User already has a comment for this recipe"
    }
    ```

### Edit a Comment

Update and return an existing comment.

* Require Authentication: true
* Require proper authorization: Comment must belong to the current user
* Request
  * Method: PUT
  * URL: /comments/:commentId
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "comment": "This is a not-so-great recipe!",
    }
    ```

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "id": 1,
      "userId": 1,
      "recipeId": 1,
      "review": "This is a not-so-great recipe!",
      "createdAt": "2021-11-19 20:39:36",
      "updatedAt": "2021-11-20 10:06:40"
    }
    ```

* Error Response: Body validation errors
  * Status Code: 400
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bad Request", // (or "Validation error" if generated by Sequelize),
      "errors": {
        "comment": "Comment text is required",
      }
    }
    ```

* Error response: Couldn't find a Comment with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Comment couldn't be found"
    }
    ```

### Delete a Comment

Delete an existing comment.

* Require Authentication: true
* Require proper authorization: Comment must belong to the current user
* Request
  * Method: DELETE
  * URL: /comments/:commentId
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Successfully deleted"
    }
    ```

* Error response: Couldn't find a Comment with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Comment couldn't be found"
    }
    ```

## Bookmarks

### Get all Bookmarks of the Current User

Returns all the bookmarked recipes saved by the current user.

* Require Authentication: true
* Request
  * Method: GET
  * URL: /bookmarks/current
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "Bookmarks": [
        {
          "id": 1,
          "user_id": 1,
          "title": "Breakfast Special",
          "description": "Here are the steps of making this dish",
          "created_at": "2021-11-19 20:39:36",
          "updated_at": "2021-11-19 20:39:36",
          "num_likes": 3,
          "preview_image": "my_dish.png",
          "video_url": "my_dish.avi"
        },
        {
          "id": 2,
          "user_id": 2,
          "title": "Lunch Special",
          "description": "Hope you enjoy this recipe",
          "created_at": "2021-11-19 20:39:36",
          "updated_at": "2021-11-19 20:39:36",
          "num_likes": 4,
          "preview_image": "lunch_dish.png",
          "video_url": "lunch_dish.avi"
        }
      ]
    }
    ```

### Create a Bookmark for a Recipe based on the Recipe's id

Create and return a bookmarked recipe specified by the recipe's id.

* Require Authentication: true
* Request
  * Method: POST
  * URL: /recipes/:recipeId/bookmarks
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "content": null
    }
    ```

* Successful Response
  * Status Code: 201
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Successfully bookmarked"
    }
    ```


* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

### Delete a Bookmark

Delete an existing bookmark.

* Require Authentication: true
* Require proper authorization: Bookmark must belong to the current user
* Request
  * Method: DELETE
  * URL: /recipes/:recipeId/bookmarks
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Successfully deleted"
    }
    ```

* Error response: Couldn't find a Bookmark with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Bookmark couldn't be found"
    }
    ```

### Create a Like for a Recipe based on the Recipe's id

Create and return a Like for a recipe specified by the recipe's id.

* Require Authentication: true
* Request
  * Method: POST
  * URL: /recipes/:recipeId/likes
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "content": null
    }
    ```

* Successful Response
  * Status Code: 201
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Successfully liked"
    }
    ```


* Error response: Couldn't find a Recipe with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Recipe couldn't be found"
    }
    ```

### Delete a Like from a Recipe based on the Recipe's id

Delete an existing like.

* Require Authentication: true
* Require proper authorization: Bookmark must belong to the current user
* Request
  * Method: DELETE
  * URL: /recipes/:recipeId/likes
  * Body: none

* Successful Response
  * Status Code: 200
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Successfully deleted"
    }
    ```

* Error response: Couldn't find a Bookmark with the specified id
  * Status Code: 404
  * Headers:
    * Content-Type: application/json
  * Body:

    ```json
    {
      "message": "Like couldn't be found"
    }
    ```
=======
